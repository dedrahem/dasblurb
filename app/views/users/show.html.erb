<%provide(:title, @user.user_name)%>
<!-- < #% provide(:title, @user.user_name)%> -->
<%= puts " " %>
<%= puts "Status: users/show.html.erb will render  " %>
<%= puts "Status:  @posts to views/post/_post.html.erb at 559" %>
<%= puts " " %>
<div class="row">
    <aside class="col-md-4">
      <section class="user_info">
        <h1>
          <%= @user.user_name %>, <%= @user.email %>
        </h1>
        <!-- < # % provide (:title, @user.user_name) %> -->
      </section>
    </aside>
    <div class="col-md-8">
      <% if @user.posts.any? %>
        <h3>dasBlurbs (<%= @user.posts.count %>) </h3>
        <ol class="posts" >
          <%= render @posts %>
        </ol>
        <%= will_paginate @posts %>
      <% end %>
    </div>
</div>
<!-- the aside tag forms the sidebar, row and column are styling -->
