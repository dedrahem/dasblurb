<%provide(:title, "Profile for User")%>
<!-- < #% provide(:title, @user.user_name)%> -->
<div class="row">
    <aside class="col-md-4">
      <section class="user_info">
        <h1>
          <%= @user.user_name %>, <%= @user.email %>
        </h1>
        <!-- < # % provide (:title, @user.user_name) %> -->
      </section>
    </aside>
  </div>
<!-- the aside tag forms the sidebar, row and column are styling -->
