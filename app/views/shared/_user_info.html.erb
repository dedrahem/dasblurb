<!-- partial for user info sidebar -->
<%= puts "STATUS : shared/_user_info partial form, as the aside class "%>
<%= puts "STATUS : routed from homepage/dashboard.html.erb            "%>
<h1><%= current_user.user_name %></h1>
<span><%= link_to "View My Profile", current_user %></span>
<span><%= pluralize(current_user.posts.count, "post") %></span>
