<!-- the @feed_items instance variable is defined in the homepage#index  -->
<!-- as # @feed_items = current_user.feed.paginate(page: params[:page])  -->
<!-- Hompage Index is intended to be the User's Timeline or Feed         -->
<!-- How did we get here ? From the hompage/index.html view using        -->
<!--  render 'shared/feed'                                               -->
<%=puts "In shared/_feed.html as rendered, @feed_items.inspect is : #{@feed_items.inspect}   !"%>
<% if @feed_items.any? %>
  <ol class="microposts">
    <%=render @feed_items%>
  </ol>
  <%=will_paginate @feed_items%>
<% end %>
